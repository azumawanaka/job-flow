'use client';

import { ChapterContent } from '@/styles/page.styles';
import HeaderTitle from '../shared/HeaderTitle';
import UnderConstruction from '../UnderConstruction';

const Projects = () => {
  return (
    <ChapterContent>
      <HeaderTitle title='Get in Touch' page='04' />

      <UnderConstruction />
    </ChapterContent>
  );
};

export default Projects;
